
<!-- <div class="container">
  <div class="row"> -->
    <%= simple_form_for [experience, experience_preferences_category], wrapper_mappings: {check_boxes: :horizontal_collection} do |f| %>
      <div class="d-flex justify-content-center mt-2">
        <div class="row"style='width: 800px; margin-top: 30px'>
          <% collection.each do |category| %>
            <div class="col-3" >
              <%= f.input :category, as: :check_boxes, collection: [category], label: false %>
            </div>
          <% end %>
        </div>
      </div>
      <%= f.button :submit, class: "btn-g smallbtn fixed-bottom", value: "Finaliser Mes Preferences", id: "confort-btn" %>
    <% end %>
  <!-- </div>
</div>
 -->
